log:
  level: info
  format: json
  output: stdout

grpc:
  name: "grpcserver"
  addr: ":9000"
  shutdown_timeout: 10s
  # 本地调试用，线上应当关掉：反射会把完整的服务描述暴露给任何能连上这个端口的人
  enable_reflection: true

db:
  driver: sqlite
  dsn: "file:greeter.db"
  max_open_conns: 10
